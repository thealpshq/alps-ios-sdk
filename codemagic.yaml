workflows:
  ios-sdk-build:
    name: Alps iOS SDK Build
    instance_type: mac_mini_m2
    max_build_duration: 60
    environment:
      xcode: 15.4
    scripts:
      - name: Build iOS SDK
        script: |
          swift build -c release
    artifacts:
      - .build/release/**
